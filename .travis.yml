# .travis.yml setup
sudo: enabled
os: linux
dist: trusty
language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.5-dev"
  - "3.6"
  - "3.6-dev"
  - "3.7-dev"

# set up dependencies
install:
  - ls -l
  - ./run_favites_docker.py -u -c example/example_config.json
